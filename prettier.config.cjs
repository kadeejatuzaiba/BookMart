module.exports = {
  semi: true, // Adds ; at the end of statements
  singleQuote: true, // Uses 'single quotes' instead of "double"
  tabWidth: 2, // 2 spaces for indentation
  trailingComma: 'es5', // Adds commas in objects/arrays when needed
  plugins: ['prettier-plugin-ejs'], // Makes Prettier format .ejs files
};
